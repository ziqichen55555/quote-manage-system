<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Remove the default "My Pipeline" filter from the CRM Pipeline view -->
    <record id="crm.crm_lead_action_pipeline" model="ir.actions.act_window">
        <field name="context">{
                'default_type': 'opportunity',
        }</field>
    </record>
</odoo>
