<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_merge_wizard_form" model="ir.ui.view">
        <field name="name">product.merge.wizard.form</field>
        <field name="model">product.merge.wizard</field>
        <field name="arch" type="xml">
            <form string="Merge existing products">
                <div class="alert alert-info" role="alert">
                    <p class="mb-2"><strong>Fix old combined shop listings (no CSV needed for this step).</strong></p>
                    <p class="mb-1">This archives products that still use a <strong>Configuration</strong> dropdown — for example one ThinkPad T490s page with options <code>20NYS4CP00</code>, <code>20NYS4CP00-8G-256G-T</code>, <code>20NYS4CP00-8G-512G-T</code>.</p>
                    <p class="mb-0">After running this, use <strong>Upload inventory CSV</strong> to recreate <strong>one listing per MTM/SKU</strong>.</p>
                </div>
                <group invisible="not result_message">
                    <field name="result_message" nolabel="1"/>
                </group>
                <footer>
                    <button name="action_merge" string="Merge existing products" type="object" class="btn-primary" confirm="Archive old combined Configuration dropdown listings? Re-upload MERGED CSV afterward."/>
                    <button string="Close" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_product_merge_wizard" model="ir.actions.act_window">
        <field name="name">Merge existing products</field>
        <field name="res_model">product.merge.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_product_merge_wizard"
              name="Merge existing products"
              parent="stock.menu_stock_inventory_control"
              action="action_product_merge_wizard"
              sequence="24"
              groups="stock.group_stock_user"/>

    <record id="product_import_wizard_form" model="ir.ui.view">
        <field name="name">product.import.wizard.form</field>
        <field name="model">product.import.wizard</field>
        <field name="arch" type="xml">
            <form string="Upload inventory CSV">
                <group>
                    <field name="filename" invisible="1"/>
                    <field name="file" filename="filename"/>
                </group>
                <div class="alert alert-info" role="alert">
                    <p class="mb-2"><strong>Additive import — safe to re-upload.</strong></p>
                    <ul class="mb-0">
                        <li><strong>New MTM/SKU</strong> in the file → creates a product + stock</li>
                        <li><strong>Existing MTM/SKU</strong> → adds new serials/quantity only (name, price, images unchanged)</li>
                        <li><strong>Not in the file</strong> → left completely untouched (stock is not zeroed)</li>
                    </ul>
                </div>
                <group invisible="not result_message">
                    <field name="result_message" nolabel="1"/>
                </group>
                <footer>
                    <button name="action_import" string="Upload and add" type="object" class="btn-primary"/>
                    <button string="Close" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_product_import_wizard" model="ir.actions.act_window">
        <field name="name">Upload inventory CSV</field>
        <field name="res_model">product.import.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_product_import_wizard"
              name="Upload inventory CSV"
              parent="stock.menu_stock_inventory_control"
              action="action_product_import_wizard"
              sequence="25"
              groups="stock.group_stock_user"/>

    <record id="product_template_tree_upload_csv" model="ir.ui.view">
        <field name="name">product.template.tree.upload.csv</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <xpath expr="//tree/header" position="inside">
                    <button name="%(action_product_merge_wizard)d"
                            string="Merge existing products"
                            type="action"
                            class="btn-primary"
                            display="always"
                            groups="stock.group_stock_user"/>
                    <button name="%(action_product_import_wizard)d"
                            string="Upload inventory CSV"
                            type="action"
                            class="btn-secondary"
                            display="always"
                            groups="stock.group_stock_user"/>
            </xpath>
        </field>
    </record>
</odoo>
